# Copy to ".env" (git-ignored) and set a real local PostgreSQL connection string:
#   cp .env.example .env
# Create the database first, e.g.  createdb threadbase
DATABASE_URL="postgresql://postgres:tanish1707@localhost:5432/threadbase?schema=public"
